[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "text-utils"
version = "0.1.0"
description = "Tiny text-processing helpers"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
